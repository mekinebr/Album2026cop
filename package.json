{
  "name": "album-copa2026-apk",
  "version": "1.0.0",
  "description": "Álbum Copa 2026 - APK Android",
  "scripts": {
    "sync": "npx cap sync android",
    "build:android": "npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/cli": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  }
}